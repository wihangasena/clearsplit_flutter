name: clearsplit_backend
description: Local API for the ClearSplit Flutter app.
publish_to: 'none'

environment:
  sdk: ^3.11.5

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^6.0.0
